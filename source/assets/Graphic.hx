class Graphic {
	/* All Graphical Assets go here


	 Instead of this:
	     Player.loadImage('assets/images/player_image.png');

	 We can do this:
	     Player.loadImage(Graphics.PLAYER);

	 This gives us compile time checking of resources and thus less bugs/errors with resources
}