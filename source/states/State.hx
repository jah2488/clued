package states;

import flixel.FlxState;

/* Keeping with the idea of isolating our game from the Flixel
   framework in a small way. I'm injecting a state into the
   inheritance heirarchy to give us a single place to make changes
   that will cascade in all our states */
class State extends FlxState {
	
}